inations with duplication allowed:");
        // for (int i = 0; i < a.length; i++) {
        //     for (int j = 0; j < a.length; j++) {
        //         for (int k = 0; k < a.length; k++) {
        //             for (int l = 0; l < a.length; l++) {
        //                 System.out.println("" + a[i] + a[j] + a[k] + a[l]);
        //             }
        //         }
        //     }
        // }
